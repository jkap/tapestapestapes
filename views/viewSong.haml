%h3
	=@song.title
	by
	%a{:href => '/view/artist/' + @song.artist, :class => 'general'}=@song.artist
Member of tapes:
%ul#songList
	- i = 0
	- @tapes.each do |tape|
		- if i % 2 == 0
			%li.song1{:onclick => 'document.location="/view/tape/' + tape.id.to_s() + '"'}=tape.title
		- else
			%li.song2{:onclick => 'document.location="/view/tape/' + tape.id.to_s() + '"'}=tape.title
		- i += 1